<Window x:Class="Firewall.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Firewall Singulier" Height="500" Width="800">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Firewall Singulier — Traffic Monitor"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0 0 0 10"/>

        <ListBox x:Name="TrafficList"
                 Grid.Row="1"
                 FontFamily="Consolas"
                 FontSize="14"/>
    </Grid>
</Window>
